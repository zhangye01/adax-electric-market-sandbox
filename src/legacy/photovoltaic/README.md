# Legacy Photovoltaic Prototype

This directory preserves the earlier photovoltaic declaration training prototype as reference material.

It is not part of the active ADAX v0.1 engineering baseline.

Do not import files from this directory into the active ADAX retail flow. If a future feature needs any logic here, migrate the needed behavior into `src/domain/**`, `src/data/**`, `src/services/**`, or active components with tests first.
